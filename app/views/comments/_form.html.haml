= form_for [referer, Comment.new], :remote => true do |f|
  = f.hidden_field :commentable_id, :value => referer.id
  = f.hidden_field :user_id, :value => current_user.id
  = f.text_area :content, :placeholder => "Escriu el teu comentari"
  = f.submit "Afegeix comentari", :class => "button"